{
  "name": "logger-ts",
  "projectType": "library",
  "root": "packages/logger-ts",
  "sourceRoot": "packages/logger-ts/src",
  "tags": ["scope:shared", "type:lib", "lang:ts"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm build",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    }
  }
}
