{
  "name": "postgres-connector-ts",
  "projectType": "library",
  "root": "packages/postgres-connector-ts",
  "sourceRoot": "packages/postgres-connector-ts/src",
  "tags": ["scope:shared", "type:lib", "lang:ts"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm build",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    }
  }
}
